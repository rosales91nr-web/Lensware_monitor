# Datos generados en runtime — NO subir al repositorio
/cache/
/backups/
/logs/
/uploads/
/data/          # almacenamiento persistente en Replit (backups, cache, logs, staging)

# Archivos locales de debug
debug.php
fix_cache.php
create_cache.php

# Editor / OS
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Secrets (nunca subir)
.env
*.env.local
