[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "/usr/bin/supervisord -c /etc/supervisord.conf"
healthcheckPath = "/health"  # 👈 CAMBIADO: antes era /api.php?action=status
healthcheckTimeout = 30
restartPolicyType = "on_failure"
