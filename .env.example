# Variables de entorno para Railway
# Configúralas en Railway → tu proyecto → Variables

# Clave secreta para el endpoint de subida de CSV desde Windows
UPLOAD_SECRET=cambia_esto_por_una_clave_segura

# Tiempo de vida del caché en segundos (default: 60)
CACHE_TTL=60
